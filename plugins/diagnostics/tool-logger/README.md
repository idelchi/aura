# tool-logger

Diagnostic plugin. Logs a summary after every tool call — tool name, result preview, and error details if the call failed. Also fires on LLM provider errors with context usage stats.

Disabled by default.
